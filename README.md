# iteathen.github.io

Static public discovery surface for iteathen open-source repositories.

The deployed endpoint is:

`https://iteathen.github.io/llms.txt`

The semantic authority for the account-wide index is `iteathen/.github/llms.txt`. This repository mirrors that index for generic HTTPS/agent access and should not become a competing source of repository metadata.
